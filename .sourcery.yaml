# Managed Sourcery baseline for emptyinkpot repositories.
# Dashboard review language/sections are configured in Sourcery itself; this
# file keeps PR review noise low and adds explicit skip labels for exceptional PRs.

clone_detection:
  min_lines: 6
  min_duplicates: 2
  identical_clones_only: false

rule_settings:
  enable:
    - default
  rule_types:
    - refactoring
    - suggestion
    - comment
  python_version: '3.9'

github:
  request_review: author
  sourcery_branch: sourcery/{base_branch}
  ignore_labels:
    - skip-sourcery
    - no-sourcery
    - sourcery-ignore

ignore:
  - _artifacts
  - .cache
  - .git
  - .next
  - .nuxt
  - .output
  - .svelte-kit
  - .tox
  - .turbo
  - .venv
  - .vercel
  - artifacts
  - build
  - coverage
  - dist
  - env
  - generated
  - node_modules
  - venv